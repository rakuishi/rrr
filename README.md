# RRR: Run, Rise, Roar

A GPS running tracker for Android.

## Setup

```bash
cp local.properties.example local.properties
```

Open `local.properties` and set your Google Maps API key for `MAPS_API_KEY`.
